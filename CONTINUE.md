# 교육부 위키 — 새 채팅방에서 이어가는 법

## 지금까지 한 일
- 교육부 보도자료(2026-07-08 ~ 2026-07-29, 총 42건)를 위키 형태(목차+문단, 나무위키 스타일)로 변환.
- 조직도 기반 분류 체계로 자동 분류(초중등교육/고등교육/평생·직업교육/유아교육·보육/디지털·인공지능교육/학생지원·복지/국제교육협력/교육행정·정책총괄/미분류).
- `index.html`을 열면 전체 결과 확인 가능.
- (2026-07-29) 한때 PWA(설치 가능한 웹앱)로 전환했었으나(manifest.json, service-worker.js, offline.html, assets/app.js, assets/icons/, serve.py), **다시 순수 정적 HTML로 되돌림**(2026-07-30). 위 파일들은 모두 삭제했고, `generate_wiki.py`의 세 템플릿에서 manifest link/apple-touch-icon/theme-color/service-worker 등록 스크립트를 제거함. `file://`로 그냥 열어도 됨(서버 불필요).
- (2026-07-30) 2024년 샘플 문서(2024-hwabangi-jinrocheheom) 삭제 — 현재 총 42건.
- (2026-07-30) **각 문서에 "배경/맥락" 문단 추가**: `data/<slug>.meta.json`의 `background` 필드(문자열)에 제도의 유래·법적근거·연혁 등을 적어두면, 부제목 아래 파란 테두리 박스(`.wiki-background`, CSS에 정의)로 표시됨. 42건 전부 채워둠(`add_backgrounds.py`가 그 작업을 한 1회성 스크립트 — 새 문서 추가 시 같은 패턴으로 재사용 가능). 배경 문단은 반드시 원문에 있던 사실만 쓰고 추측/외부지식으로 없는 사실을 지어내지 말 것(예: "매년 X월 X일에 열린다" 같은 미확인 정기성 주장 금지).
- (2026-07-30) **나무위키식 키워드 자동 링크** 추가: `generate_wiki.py`의 `KEYWORD_LINKS` 리스트에 `{"phrase": "문구", "targets": {소스slug: 링크할slug}}` 형태로 등록하면, 그 문구가 본문(배경 포함)에 처음 등장하는 곳만 자동으로 다른 문서 링크가 됨(`apply_keyword_links()` 함수). 지금은 43개 기존 문서끼리만 연결하는 정책이라 새 키워드 "허브 문서"는 만들지 않음 — 새 문서 추가 시 기존 문서와 겹치는 고유명사/프로그램명이 있으면 KEYWORD_LINKS에 항목을 추가할 것.
- (2026-07-30) **중요 버그 수정**: `is_title_chunk()`가 제목 앞 8글자가 본문 "어디에" 있든 매칭해버려서, 제목의 키워드가 본문 중간에 재등장하면(예: 제목이 "3대 메가프로젝트..."인데 본문 뒷부분에서도 "3대 메가프로젝트 관련 분야에서는"처럼 다시 언급되는 경우) 그 문단 전체가 통째로 사라지는 버그가 있었음. 문단이 제목으로 "시작하는지"만 보도록 고쳐서(`startswith`) 42건 전체 재빌드함 — 이 버그로 인해 이전에 빠졌던 문단들이 복원됨.
- (2026-07-30) 모바일 CSS 개선(`assets/style.css`의 `@media (max-width: 600px)` 블록): 여백/폰트크기/터치 영역 확대, 표 가로스크롤 관성 스크롤 추가. 레이아웃/색상 구조는 그대로(나무위키 스타일 유지 요청에 따름).
- (2026-07-30) **디자인 전면 개편**: "백과사전식/수수함" 피드백에 따라 카드형 레이아웃(분류=카드 그리드, 문서목록=카드 리스트)으로 교체, 인디고 포인트 컬러(`--accent`) 도입. `generate_wiki.py`의 INDEX_TEMPLATE/CATEGORY_PAGE_TEMPLATE/PAGE_TEMPLATE 및 `assets/style.css` 전면 수정. (처음엔 Pretendard 폰트를 외부 CDN `@import`로 불러왔었는데, 카카오톡 인앱 브라우저에서 그 요청이 막히면서 CSS 전체가 안 먹는 문제가 있어서 **외부 리소스 의존성 없이 시스템 폰트만 쓰도록 되돌림** — 외부 CDN @import/폰트는 다시 넣지 말 것.)
- (2026-07-30) **GitHub Pages로 배포 완료**: https://greenday0320.github.io/moe-wiki/ — 정식 HTTPS 주소라 카톡 포함 어디서든 정상 작동. 로컬 LAN 서버(`python -m http.server`)와 Cloudflare 임시 터널(`cloudflared`)은 배포 전 모바일 테스트용으로 잠깐 썼던 것이고 지금은 정리(kill)해서 안 떠 있음 — 더 이상 필요 없음. 자세한 배포/업데이트 방법은 아래 "⚠️" 섹션 참고.
- (2026-07-30) **업데이트 전파 여부 확인**: `git push`만 하면 링크(URL)는 그대로 유지된 채 내용만 자동 갱신됨 — 공유받은 사람이 새 링크를 다시 받을 필요 없음. `gh api repos/Greenday0320/moe-wiki/pages` 확인 결과 `build_type: "legacy"`라 push하면 GitHub Pages가 자동 재빌드(보통 1분 내외). 단 실제 응답 헤더(`curl -sI https://greenday0320.github.io/moe-wiki/`) 확인 결과 앞단 CDN(Fastly)이 `Cache-Control: max-age=600`을 걸어두고 있어서, push 직후 방문자는 최대 약 10분간 이전 버전을 볼 수 있고 이후 자동 갱신됨(강제 새로고침 불필요). 이 캐시 헤더는 GitHub Pages 인프라가 고정으로 설정하는 값이라 저장소 쪽 설정으로 줄일 수 없음.
- (2026-07-30) **6월치(2026-06-01~06-30, 55건) 추가**: `fetch_batch.py --days 60`으로 5/31~7/30 목록 재수집(타임아웃 2회 발생 — 사이트가 가끔 느림, 재시도하면 됨; 다운로드된 raw 파일은 재실행해도 스킵되므로 안전). 55건을 3개 배치(19/18/18)로 나눠 서브에이전트 3개를 병렬로 돌려 파이프라인(2단계) 처리 — 문서량이 많을 때는 이 방식이 컨텍스트를 아낌. 현재 총 **97건**. 미분류 6건(106592/106591/106464/106462 — 평가원(KICE) 발표라 "책임자/담당자" 표 자체가 없는 별도 양식; 106440/106424 — 타 부처가 담당부서 표에서 먼저 나오는 공동 보도자료) 전부 기존에 문서화된 한계 케이스와 동일 패턴. `KEYWORD_LINKS`에 6월 문서 몇 건 추가 연결(계약학과→106326, 지역성장 인재양성→106345/106504, 데이터로 읽는 우리 교육 제7~9호 연쇄링크 106436→106571→106680).
- (2026-07-30) **분류 페이지에 중분류(하위분류) 그룹핑 추가**: 그동안 `category_map.json`의 `children`(중분류 목록)이 칩(chip)으로 상단에 표시만 되고, 정작 문서 목록은 flat하게 나열돼 있었음. `generate_wiki.py`의 `rebuild_categories()`를 고쳐서, `children`이 있는 분류(초중등교육/고등교육/평생·직업교육/학생지원·복지)는 이제 중분류별 `<h2>` 섹션(`.subcat-section`, `_subcat_section()` 함수)으로 나눠서 렌더링하고, 중분류가 없거나(`null`) `children` 목록에 없는 문서는 맨 아래 "기타" 섹션으로 묶임. `children`이 아예 없는 분류(유아교육·보육/디지털·인공지능교육/국제교육협력/교육행정·정책총괄/미분류)는 기존처럼 flat 목록 그대로. `CATEGORY_PAGE_TEMPLATE`도 `{rows}` 대신 `{content}`(그룹 섹션 전체 HTML 또는 flat 목록)를 받도록 바꿈. CSS에 `.subcat-section` 여백 추가하면서 `CSS_VERSION`을 2→3으로 올림(모바일 캐시 무효화 관례).
- (2026-07-30) **"정책 위키"(주제별 종합 문서) 신설**: 개별 보도자료 1건=문서 1개였던 기존 구조와 별개로, 여러 보도자료를 관통하는 정책/사업 주제를 사람이(Claude가) 직접 종합해서 쓰는 새로운 문서 유형을 추가함. "보도자료를 요약한 게 아니라 핵심을 뽑아 위키로 재구성해서, 그것만 읽어도 교육부가 지금 뭘 하는지 한눈에 보이게 해달라"는 사용자 요청으로 시작.
  - **데이터**: `data/topics/<topic-id>.json` — `{id, title, subtitle, period, related: [boardSeq...], sections: [{heading, body}...]}`. `body`는 일반 텍스트(`render_prose()`로 렌더링, `\n\n`=문단, `**bold**` 지원). 개별 보도자료의 `data/<slug>.meta.json`과 달리 kordoc 청크 기반이 아니라 완전히 수기 작성.
  - **렌더러**: `generate_wiki.py`에 `render_topic(topic_id)`(→ `topics/<id>.html` 생성), `_collect_topics()`, `TOPIC_PAGE_TEMPLATE` 추가. `--rebuild-index` 실행 시 `data/topics/*.json`을 전부 다시 렌더링함. 단발성으로 하나만 다시 만들 때는 `python generate_wiki.py --topic <id>`.
  - **배치**: index.html 최상단(헤더 바로 아래, "분류" 섹션보다 위)에 "정책 위키" 카드 그리드(`.topic-grid`/`.topic-card`, `category-card`와 톤은 비슷하되 강조 테두리로 구분) 신설. 개별 분류 페이지는 건드리지 않음(사용자가 "분류는 유지하되 추가로 만들어달라"고 명시).
  - **주제 선정 방식**: 97건을 grep으로 스캔해 3건 이상 보도자료에 걸쳐 반복 등장하는 정책명 후보 8개를 뽑아 사용자에게 제시 → 사용자가 이번 라운드 3개(①지역균형 고등교육 혁신 `regional-higher-edu` ②첨단분야 인재양성 `advanced-industry-talent` ③직업교육 혁신 `vocational-edu-innovation`)를 선택. 나머지 5개 후보(다문화·이주배경학생/한국어교육 국제화/사교육·학원비 대책/수능 일정 총정리/데이터로 읽는 우리 교육 시리즈)는 다음 라운드 후보로 남겨둠.
  - **작성 원칙**: 각 주제 문서는 관련 보도자료 원문을 실제로 다시 읽어(grep 매칭만으로 관련 문서를 확정하지 말 것 — 예: "계약학과"가 통계표에 스치듯 언급된 문서, "거점국립대"가 위원 약력에만 나온 문서 등은 제외) 배경→시간순 경과→항목별 현황 구조로 재구성. 숫자·날짜·기관명은 반드시 원문에 있는 것만 쓰고, 여러 보도자료에 걸친 사실관계를 잇는 문장(예: "이 정책의 법적 근거가 O월 O일 시행령 개정으로 마련됐다")도 각 문서에 실제로 그 내용이 있는지 확인 후 작성.
  - **다음에 할 일**: (a) 종합 개관 페이지(대분류별 브리핑 1페이지, 사용자가 "주제 문서부터 먼저"라고 해서 이번엔 보류)는 아직 없음. (b) 새 보도자료가 들어올 때마다 관련 주제 문서의 "관련 보도자료"·본문에 반영할지 여부는 매번 판단 필요(자동화 안 됨). (c) 개별 보도자료 문서에서 관련 주제 위키로 되돌아가는 링크는 아직 없음(`KEYWORD_LINKS`처럼 자동 연결하는 장치 미구현).
- (2026-07-31) **5월치(2026-05-03~05-31, 61건) 추가**: 사용자가 "정책 위키를 제대로 구축하려면 보도자료가 더 쌓여야 할 것 같다"며 5월분 추가를 요청. `fetch_batch.py --days 92`로 시도했으나 사이트 응답 지연으로 타임아웃이 반복됨(매니페스트가 전체 성공해야만 끝에 한 번 저장되는 구조라, 중간에 죽으면 그 실행 전체가 날아감) → **`fetch_batch.py`에 재시도 로직 추가**: `_retry()` 헬퍼(최대 4회, 3초 간격 재시도)로 `fetch()`와 첨부파일 다운로드를 감쌌고, 페이지 단위로 매니페스트를 중간 저장하도록 바꿈(`main()` 루프 안 `page += 1` 직후). 이후 정상적으로 61건 전부 수집. 이제 총 **158건**.
  - 61건을 3개 배치(21/20/20)로 나눠 서브에이전트 병렬 처리(6월치와 동일한 파이프라인).
  - **`extract_contact()` 파싱 오류 5건을 수동 수정**(전부 데이터 오류이지 로직 버그 아님 — `generate_wiki.py`는 건드리지 않고 각 `data/<seq>.meta.json`의 `bureau`/`division` 값만 원문 표 대조 후 직접 고침):
    - 106304: 담당부서 셀이 "교육부 교육국제화담당관"처럼 한 줄로 합쳐져 있어 division이 "사무관"으로 잘못 추출 → "교육국제화담당관"으로 수정.
    - 106307, 106106: 담당부서 셀이 "교육부\<br\>대학지원관\<br\>지역대학지원과"처럼 3줄인데 `extract_contact()`는 2줄(부서/과) 기준이라 중간 줄(대학지원관)을 division으로 잘못 추출 → bureau="대학지원관", division="지역대학지원과"로 수정.
    - 106236: 담당부서 표 2번째 행에 앞쪽 빈 셀(placeholder `<td></td>`)이 없어서 "재외교육지원담당관"이 아니라 "담당자"라는 직함 레이블이 division으로 잘못 추출 → 수정.
    - 106276: "담당 부서"가 아니라 "담당 기관"(국립특수교육원 등 소속기관 공동주최) 표 형식이라 division이 "담당자"로 잘못 추출 → "디지털교육지원과"로 수정(다만 국립특수교육원은 `category_map.json`에 없는 소속기관이라 분류 자체는 여전히 미분류 — 이건 버그가 아니라 실제로 매핑이 안 된 것이므로 정상).
    - 수정 후 4건(106304/106307/106106/106236)은 정상 분류됨, 106276만 미분류 유지(정당한 미분류).
  - 최종 미분류 3건만 남음 — 전부 기존에 문서화된 패턴: 106292(평가원 KICE 양식, 담당자 표 자체 없음), 106108(보건복지부 공동발표, 타부처가 표에 먼저 나옴), 106276(위 소속기관 케이스).
  - `KEYWORD_LINKS`에 5월 문서 추가 연결: 계약학과→106147, 지역성장 인재양성(앵커)→106145/106278/106307, 데이터로 읽는 우리 교육 시리즈 체인을 제4호(106054)~제10호까지 전부 연결(106054→106180→106324→106436→106571→106680→106773).
- (2026-07-31) **정책 위키 3건 보강**(5월치 추가로 관련 문서가 늘어난 데 따른 재보강 — "정책 위키를 제대로 구축하려면 자료가 더 쌓여야 한다"는 사용자 판단으로 5월분을 먼저 채운 뒤 진행): grep으로 전체 158건을 다시 스캔해 기존 3개 주제와 겹치는 5월 문서를 찾고, 원문을 읽어 진짜 관련 있는 것만(예: "IP 마이스터"는 마이스터고와 무관한 동명이의라 제외) `related`에 추가하고 본문에도 사실을 반영함.
  - `regional-higher-edu`(6→8건): 106221(거점국립대 인문사회 연구소 3개교, 5.20), 106254(글로컬대학 성과평가 유튜브 생중계 첫 도입 예고, 5.25 — 이후 106590의 결과발표로 이어지는 전조 기사) 추가.
  - `advanced-industry-talent`(7→13건): 106106(해양수도권 인재양성 교육부-해수부 협약), 106145(앵커 연차점검 계획, 4천억 차등배분), 106147(제4기 국가산학연협력위원회 출범 — 계약학과·거점국립대·앵커가 하나의 시행계획 아래 있음을 보여줌), 106278(전남광주 앵커 현장소통 간담회), 106282(한미 반도체 학생 교류 행사), 106307(한밭대 앵커 현장방문) 추가. "① 정원제도 유연화와 앵커 체계" 섹션에 앵커의 실제 지역 rollout 사례들을 새 문단으로, "② 계약학과" 섹션 앞에 국가산학연협력위원회 관련 문단을 추가.
  - `vocational-edu-innovation`(5→7건, 5→6개 프로그램): 106223(초중등교육법 시행령 개정으로 협약형 특성화고에 '지역특화 특성화고등학교' 법적 근거 마련, 5.20 — ③ 섹션에 반영), 106134(직업계고 채용연계형 직무교육과정, 2022년 시작·2026년 3,000명 확대 — 기존 5개 축과 결이 달라서(학교 단위가 아니라 학생 개인 단위) 새 섹션 ⑥으로 추가).
  - 세 문서 모두 `period`를 "2026년 6~7월"→"2026년 5~7월"로 갱신.
- (2026-07-31) **정책 위키 4번째 주제 추가**: `유보통합·영유아정책|돌봄`으로 158건 전체를 재스캔하니 21건이 걸렸는데, 원문 대조 후 실제 무관한 6건(106581/106577/106407/106397/106327/106293 — "학교복합시설사업"·"소규모학교 혁신" 등 완전히 다른 정책 주제인데 "돌봄"이라는 단어만 스치듯 들어간 케이스, "청소년 자살예방"의 "돌봄·간병 부담"은 가족돌봄청소년 복지 얘기라 무관)를 제외하고 14건으로 `early-childhood-care`(정부책임형 유보통합과 영유아 돌봄 확대) 신설. 5개 섹션: ①정부책임형 유보통합(유보통합법 정비, 교육청-지자체 합동연수) ②거점형·연계형 돌봄기관과 틈새돌봄(유치원 시니어돌봄사, 어린이집 아침돌봄, 돌봄종사자 처우) ③보육 인프라·안전·건강(직장어린이집, 안전영상, 건강증진사업) ④영유아 사교육 인식개선 캠페인(5월 학회협약→6월 릴레이챌린지→7월 육아정책포럼, 시간순 3편) ⑤온동네 초등돌봄·교육(영유아 넘어 초등까지). 이걸로 정책 위키 총 4건.
- **다음 라운드 후보로 남은 것**(158건 기준 재집계): 한국어교육 국제화(14건), 사교육·학원비 대책(11건, 유보통합 후보와 일부 겹침 주의), 데이터로 읽는 우리 교육 시리즈 모아보기(7건, 이미 KEYWORD_LINKS로 체인 연결됨 — 목록형이라 만들기 가장 가벼움), 수능 일정 총정리(6건).
- (2026-07-31) **홈 상단 안내 배너 + 조직도 페이지 추가**:
  - `index.html` 헤더 바로 아래에 `.site-notice` 박스 신설(제작자 공지: 격주 업데이트, 업데이트하면 같은 링크로 자동 반영된다는 것, 정책위키/분류/최신문서 구성 설명, "(제작 by NSG)" 서명). 순수 정적 사이트라 실제로는 push해야 반영되지만 "콘텐츠 자동 생성"이 아니라 "배포(같은 링크로 최신본 확인 가능)"를 뜻한다는 걸 사용자와 확인 후 문구 작성. `CSS_VERSION` 4→5.
  - `org.html`(사이트 루트, index.html과 같은 레벨) 신설 — 교육부 실제 조직도(장관 직속 실/국/관 → 과/팀, 최대 3단계)를 나무 구조로 보여주고, 각 과/팀 옆에 그 부서명(`meta.json`의 `division` 필드와 정확히 일치)으로 작성된 보도자료 건수를 표시. 건수>0인 과/팀은 `classify()`로 알아낸 분류 페이지로 링크됨. 조직도 원본은 `https://www.moe.go.kr/sub/infoRenewal.do?m=0604&page=0604&s=moe`를 WebFetch로 2차례 조회해 확보(고등평생정책실·인공지능인재지원국 산하 등 실제 계층을 확인), `category_map.json`의 division_map(65개 과/팀)·bureau_map(19개 실/국/관)과 대조해 `org_chart.json`(새 파일, 사이트 루트)에 중첩 트리로 저장. 렌더링은 `generate_wiki.py`의 `render_org()`/`_render_org_node()`/`_org_count()`/`_org_href()`가 담당하며 `--rebuild-index` 실행 시 함께 갱신됨.
  - 일부 실/국의 하위 부서 배치는 실제 조직도를 100% 재확인하지 못한 부분(예: 기획조정실 직속 vs 대변인 산하 담당관 배치)이 있어 대략적인 그룹핑일 수 있음 — "한눈에 보기용" 참고 자료로 안내함(법적 정확성을 요하는 용도로는 원본 링크를 봐야 함).
  - index.html에는 안내 박스 바로 아래 "🏛️ 교육부 조직도 한눈에 보기" 링크 카드 한 줄만 추가(사용자가 "별도 페이지로" 배치를 택함).

- (2026-08-01) **조직도 클릭 시 부서 단위 문서 목록으로 이동하도록 수정**: 기존엔 `org.html`에서 과/팀(leaf)을 누르면 그 부서가 속한 **대분류 카테고리 페이지**(예: `categories/admin-policy.html`)로 연결돼, 하위분류가 없는 대분류(교육행정·정책총괄 등)에서는 그 대분류 산하 모든 부서의 문서가 뒤섞여 나오는 문제가 있었음(예: "기획담당관"(1건)을 눌러도 "정책기획관" 산하 5개 과 전체 문서(7건)가 다 보임). `generate_wiki.py`에 `_write_org_node_page()`를 추가해 leaf 노드마다 `org/<부서명>.html`을 새로 생성 — `meta.json`의 `division` 값이 그 부서명과 **정확히 일치하는** 문서만 모아서 보여줌(제목에 조직 경로 "기획조정실 > 정책기획관 > 기획담당관" 표시). `_render_org_node()`가 트리를 순회하며 조상 경로(path)를 함께 넘기도록 고쳤고, 기존에 카테고리 페이지로 연결하던 `_org_href()`는 더 이상 안 쓰여서 제거함. 상위 조직(실/국/관)은 기존처럼 링크 없이 합계 건수만 표시(범위 밖 — 필요하면 나중에 상위 조직 롤업 페이지도 추가 가능).

- (2026-08-02) **정책 위키 기존 4건 보강**(158건 데이터 추가 없이, 기존 4개 주제만 재검토 — 사용자가 "정책 위키 업데이트"를 요청하며 방향을 함께 정함): 새 보도자료가 없어 158건 전체를 다시 grep+원문 대조하는 방식으로 진행. `advanced-industry-talent`(13→16건): 5/20 발표된 '국민주권 정부 1년' 성과 종합보고(106235)에서 채용조건형 계약학과 증가폭·부트캠프 규모(50교 4,200명) 수치를 가져오고, 별도로 안 다루던 '첨단분야 혁신융합대학(COSS)' 프로그램(106566, 2021년부터 18개 분야 대학연합체·코-위크 아카데미)을 새 섹션 ⑤로 추가, 대학규제혁신 공모전(106326)에서 계약학과 학점인정 확대 사례 반영. `regional-higher-edu`(8→9건): 106235에서 "지역대학 입학 경쟁률 11.6%p 상승" 성과 수치를 개요에 추가. `early-childhood-care`(14→17건): 106235의 유치원비·어린이집비 감소율(41.4%/18.3%) 수치, 부천 유치원 교사 사망 사건을 계기로 나온 「유치원 교사 대체인력 지원 개선방안」(106448, 순회교사 법제화·대체인력풀), '책 놀이 유치원·어린이집' 독서교육 사업(106535)을 반영. `vocational-edu-innovation`은 재검토 결과 **추가할 문서 없음**(키워드로 걸린 7건 전부 원문 대조 후 무관 판정 — 이미 158건 기준으로 충분히 완결된 상태였음을 확인).
  - 재확인된 false-positive 패턴: 인명 약력에 우연히 들어간 회사 내부 프로그램명("LG전자 글로컬대학 센터장" — MOE 정책과 무관), 수능 시행계획·대학정보공시처럼 모든 학교유형을 나열하는 표에 스치듯 들어간 학교급 명칭("특성화고" 등). 이런 건 grep만으로 판단하지 말고 반드시 원문 문맥을 확인해야 함(이전에도 "IP 마이스터" 사례로 같은 교훈이 있었음).
  - 대분류별 개관 페이지(중분류별 브리핑 1페이지)는 사용자와 논의했으나 이번 라운드에서는 보류하기로 함 — 다음에 다시 논의 가능.

- (2026-08-02) **"업무계획" 섹션 신설** (대분류별 개관 페이지 기획은 잠깐 보류하고 사용자가 새로 요청): 교육부가 매년 초 발표하는 연간 업무계획을 요약한 새 문서 유형. 원문은 `https://www.moe.go.kr/sub/infoRenew.do?page=72762&m=031101&s=moe`(카드뉴스 10장 + 정식 pdf/hwp 첨부) — WebFetch로 먼저 요약을 받았으나 파일 다운로드 URL 등 핵심 사실은 반드시 `curl`로 원본 HTML을 직접 받아 grep 대조 후 확정함(WebFetch는 작은 모델이 요약하므로 카드 슬라이드 개수 등 디테일이 부정확할 수 있음 — 이번엔 실제로 대조해보니 다운로드 URL은 맞았지만 "이전 연도 링크 존재" 주장은 원문에서 확인 안 됨).
  - **범위 결정**(사용자와 논의): 카드뉴스 10장 기반으로 위키 문단으로 재구성(원문 hwp/pdf 전체 파싱은 안 함), 연도별 컬렉션 구조(`data/workplan/`, topics처럼 여러 개 모아보기)는 아직 안 만들고 2026년치 단일 문서로만. 정책 위키와는 명확히 구분되는 별도 섹션으로 배치(사용자가 명시).
  - **구현**: `data/workplan-2026.json`(title/subtitle/source_url + sections 배열, 각 section에 `related_topics`로 기존 정책 위키 링크 연결 가능) → `generate_wiki.py`의 `render_workplan(workplan_id)`가 `workplan-2026.html`(사이트 루트) 생성. 3대 방향(①환경변화 대응 ②국가책임 기본교육 ③헌법가치·세계화) 아래 8개 세부과제를 방향별로 묶어 문단화했고, "미래인재양성·지방대학육성"은 `advanced-industry-talent`/`regional-higher-edu`에, "국가책임 돌봄"은 `early-childhood-care`에 상호링크 연결. `--rebuild-index` 실행 시 `render_workplan("2026")`도 함께 호출됨(아직 연도가 하나뿐이라 `_collect_topics()`같은 자동 순회 없이 하드코딩 — 내년에 2027년치가 생기면 이 부분을 일반화할지 다시 판단 필요).
  - index.html에는 "🏛️ 조직도" 링크카드와 "정책 위키" 그리드 사이에 "2026년 업무계획" 제목 + 링크카드 한 줄을 새로 추가(기존 `org-link-card` 클래스 재사용, 새 CSS 없음).
  - **보류된 것**: 대분류별 개관 페이지(교육부를 전혀 모르는 사람 대상 설계로 논의 시작함 — "조직 소개+최근 정책 하이라이트" 방향까지는 정했으나 배치/범위는 미정 상태에서 중단) — 다음에 이어서 정하면 됨.

- (2026-08-02) **정책 위키 5·6번째 주제 추가**(4건→6건, 158건 기준으로 남아있던 후보 4개 중 사용자가 2개를 선택): `korean-language-edu`(한국어교육 국제화, 17건)와 `private-education-cost`(사교육·학원비 대책, 4건) 신설.
  - `korean-language-edu`: 한국어교원 해외파견(93명/13개국), 재외 한국교육원 네트워크(파라과이/인도/재미동포/유학박람회/카자흐스탄 UN평화유지군 등 국방분야 한국어보급/세계 한국어교육자 통합연수), AI '모두의 한국어' 플랫폼, 한류 활용 콘텐츠(BTS/K-드라마/러시아 K팝수업), 대학 해외진출 결합형(충남대 타슈켄트), 양자 교육외교(콜롬비아/이집트/몽골/브라질) 6개 섹션. **이주배경학생 이중언어말하기대회·다문화교육 공모전·북한배경학생 지원 4건은 의도적으로 제외**했음 — grep으로는 "한국어"/"이주배경학생" 키워드가 걸리지만, 이건 국내 다문화학생 지원 정책이지 "국제화"가 아니라서 주제 정합성을 위해 뺌(나중에 "다문화·이주배경학생 지원"이라는 별도 주제로 다룰 후보로 남겨둠).
  - `private-education-cost`: 영유아 사교육 인식개선 3건은 이미 `early-childhood-care`가 다루고 있어 제외하니 실제로는 **3건**(학원 신고포상금 인상/자기주도 학습센터/대입정보 AI챗봇·온라인상담)만 남음 — 다른 주제(6~17건)보다 훨씬 적지만, 단속강화·대안공간·정보격차해소라는 3갈래 구조가 명확해서 사용자와 상의 후 그대로 진행하기로 함. `early-childhood-care`를 향한 안내 문구만 개요에 텍스트로 남기고(하이퍼링크는 아님 — `render_topic()`이 topic 간 링크를 지원하지 않아서 굳이 기능을 새로 안 만들고 텍스트 언급으로 대체), `early-childhood-care` 쪽에서 역방향으로 연결하지는 않음.
  - 두 주제 모두 158건 전체를 grep + 원문 대조로 재확인하는 기존 방식 그대로 진행. 이제 정책 위키 총 **6건**.

- (2026-08-02) **안내 박스에 "최근 업데이트" 날짜 표시 추가**: `index.html` 상단 안내 박스에 `최근 업데이트: {YYYY-MM-DD}` 한 줄 추가. 수기로 날짜를 적어두는 대신, `rebuild_index()`가 실행되는 시점의 오늘 날짜(`date.today()`)를 자동으로 넣도록 구현 — 이 프로젝트는 항상 "내용 수정 → `--rebuild-index` → 커밋·푸시"를 같은 세션에서 바로 이어서 하므로, 빌드 시점 날짜가 곧 실제 배포일과 사실상 같음. `CSS_VERSION`을 6→7로 올림(새 `.site-notice-updated` 클래스 추가에 따른 모바일 캐시 무효화 관례).
- (2026-08-03) **3~4월치(2026-03-02~04-28, 105건) 추가**: `fetch_batch.py --days 160`으로 재수집. 이번엔 서브에이전트가 세션 API 한도(리셋 15:50 KST)에 두 차례 걸려 여러 개가 중간에 죽었음 — **각 boardSeq 단위로 파일이 즉시 디스크에 쓰이는 파이프라인 구조 덕분에 작업 유실은 없었고**, 매번 `data/*.meta.json`·`articles/*.html` 존재 여부로 "어디까지 됐는지" diff를 떠서 안 된 것만 남은 서브에이전트/직접 작업으로 재분배하는 방식으로 이어감(총 4~5차례 배치 재분배). 최종적으로 105건 전부(파싱 46건, 배경문단만 필요 29건, 렌더링만 필요 17+1건) 처리 완료, 총 **279건**(단 raw/에 이미 받아둔 2월 24~28일치 11건·8월 1~3일치 2건은 이번엔 사용자가 3~4월만 요청해서 손대지 않음 — 다음에 이어서 처리 가능).
  - **분류 감사**: 15건 미분류 전부 검증 완료 — 국무조정실 주재 관계장관회의(105980/105967), 성평등가족부·고용노동부·보건복지부·중소벤처기업부·과기정통부가 표에 먼저 나오는 공동 보도자료(105867/105999/105719/105708/105683/105734/105660), 국가데이터처 통계 발표(105597), 평가원(KICE) 단독 발표(105763/105757/105756), 담당부서 표 자체가 없는 관계부처 TF 합동자료(105839) — 전부 기존에 문서화된 정당한 미분류 패턴과 일치, 로직 버그 없음.
  - **서브에이전트 분배 실수 1건 발견·수정**: 46건을 두 그룹으로 나누는 과정에서 105886 1건이 양쪽 리스트 어디에도 안 들어가 누락됐던 것을 최종 검증 단계에서 발견, 직접 파싱·작성해 추가함(세월호 12주기 안전교육 현장방문 기사).
  - **KEYWORD_LINKS 3개 문구에 연결 추가**: "데이터로 읽는 우리 교육" 체인에 제1~3호(105614→105736→105924→106054, 기존 제4호 시작점과 연결) 추가, "한국교육원"에 105666(캄보디아 한국어교원 양성, 호치민시한국교육원 언급)→106603 연결.
  - **Windows 콘솔 인코딩 함정 재확인**: bash 도구로 `python -c "print(...)"`나 `cat`으로 한글 JSON 필드를 직접 출력하면 cp949 콘솔이라 완전히 깨져서(mojibake) 실제 값을 오판하기 쉬움(이번에 "기획조정실"로 잘못 읽어 미분류를 버그로 오인할 뻔함) — 한글 내용을 확인할 땐 항상 파일에 UTF-8로 써서 **Read 도구로 읽을 것**, bash 콘솔 출력을 그대로 믿지 말 것.
- (2026-08-03) **정책 위키 6건 전면 보강**(3~4월치 105건 추가 직후 "정책위키도 업데이트하자"는 요청으로 진행): 기존 6개 주제(early-childhood-care/private-education-cost/advanced-industry-talent/regional-higher-edu/korean-language-edu/vocational-edu-innovation)는 전부 5~7월 문서로만 구성돼 있었는데, 이번에 3~4월치가 들어오면서 각 주제의 "최초 발표/기원" 시점 문서가 새로 확보됨. 주제별 핵심 키워드로 105건을 grep한 뒤 원문을 읽어 진짜 관련 있는 것만(예: "돌봄"이 스치듯 들어간 특수학교·역사교육·유학생관리 문서, "학원"이 스치듯 들어간 수능 시행계획 등은 제외) 골라 반영. 총 39건을 6개 주제에 분배(문서당 최대 6개 주제 후보를 원문 대조로 압축).
  - **구조적으로 중요한 발견**: 기존 주제들이 인용만 하고 있던 상위 정책의 "원문 발표"가 이번에 확보됨 — early-childhood-care의 "4월 마련한 영유아 사교육 대응 방안"(105780, 전체 내용 보강), private-education-cost 전체의 뿌리인 "사교육비 경감을 위한 정책 방안"(105779, 4.1), advanced-industry-talent의 "앵커(옛 라이즈)" 명명 자체가 나온 "지역성장 인재양성체계(앵커) 추진방안"(105790, 4.2), regional-higher-edu의 "서울대 10개 만들기" 원문인 「성장엔진 연계 지역인재 양성방안」(105889, 4.15) 등. 이런 문서는 각 절 맨 앞에 원문 상세 내용을 추가하고, 기존 5~7월 서술은 "그 후속 조치"로 자연스럽게 이어지도록 문단을 재배치함.
  - **주제 간 크로스오버 판단**: 문서 하나가 여러 주제 후보에 걸리는 경우, 그 문서의 실제 프레이밍(어느 절 제목과 더 맞는지)을 기준으로 주 배치 주제 하나를 정하고 중복 인용은 피함(예: 105929 지역혁신중심 대학지원체계 시행령 입법예고는 regional-higher-edu가 아니라 advanced-industry-talent의 "배경"에 배치 — 그 주제의 배경 문단이 이미 이 시행령의 최종 국무회의 통과만 언급하고 있어서 입법예고 단계를 그 앞에 붙이는 게 더 자연스러웠음).
  - **period 필드**: 6개 주제 전부 "2026년 5~7월"(또는 "6~7월") → "2026년 3~7월"(vocational-edu-innovation만 "4~7월")로 갱신.
  - `KEYWORD_LINKS`나 `generate_wiki.py`는 건드리지 않음 — 정책 위키(`data/topics/*.json`)와 렌더러(`render_topic()`)는 이미 있는 기능이라 순수 콘텐츠 작업만 진행.
- (2026-08-03) **정책 위키 2건 신설**("정책위키에 추가할 주제 있을까?"라는 질문에 279건 전체를 키워드로 재스캔해 후보 5개(데이터로 읽는 우리 교육 시리즈/수능 일정/특수교육/외국인 유학생/학교폭력·청소년보호)를 추려 제시 → 사용자가 2개 선택): 정책 위키 총 8건으로 확대.
  - `data-read-our-education`(제1~10호 모아보기, 10건): 각 호 보도자료가 첨부파일(별첨 pdf) 안내 위주라 kordoc으로 파싱되는 본문 자체가 매우 짧음 — 그래서 "목록형"으로 설계(개요·배경 문단 + "지금까지 발간된 10개호" 절 하나에 회차·날짜·주제를 글머리표로 나열). `render_prose()`가 `▪`로 시작하는 줄이 연속되면 자동으로 `<ul>`로 바꿔주는 걸 활용(문단 구분 없이 줄바꿈만 해도 한 문단으로 묶여 리스트가 됨).
  - `special-education-support`(특수교육 지원 확대, 7건): 법·제도 정비(점자교과서 적기보급 초중등교육법 개정+민관협약, 특수교육법 2회 개정으로 행동중재전문가·연차별 설치계획 근거, 학교폭력예방법 장애학생 심의참여 의무화)/AI·디지털 학습지원(발달장애 AI교육자료, 장애학생 e페스티벌)/현장소통·인식개선(삼성화재 협약, 특수학교 현장방문) 세 갈래로 구성. 5월 7일치 「특수교육법」 등 8건 국회통과 보도자료처럼 여러 법이 한 건에 묶인 문서는 특수교육 관련 조항만 골라 인용하고 나머지(진로교육법·학술진흥법 등)는 이 주제와 무관해 제외.
  - **다음 라운드 후보로 남은 것**: 2027학년도 수능 일정 총정리(8건, 시행기본계획→모의평가 2회→채점·이의신청→시행세부계획), 외국인 유학생 유치·정착 지원(6건, 유학생 질관리체계·한국유학종합시스템·취창업지원 우수대학), 학교폭력·청소년보호(8건, 단 성격이 다른 하위주제(학교폭력/촉법소년/청소년도박/청소년자살예방)가 섞여 있어 그대로 묶기보다 재검토 필요).

## 새 채팅방에서 할 말 (예시)
> "C:\Users\82103\Desktop\교육부위키 프로젝트 이어서 작업할 거야. CONTINUE.md 읽고 최근 N일치 보도자료 추가로 처리해줘. 다 되면 git push까지 해서 GitHub Pages에 반영해줘."

## ⚠️ 이 프로젝트는 GitHub에 배포되어 있음 — 로컬 수정만으로는 사이트가 안 바뀜
- 라이브 사이트: **https://greenday0320.github.io/moe-wiki/**
- 저장소: **https://github.com/Greenday0320/moe-wiki** (기본 브랜치 이름 `master`, `main` 아님)
- `gh` CLI가 이미 `Greenday0320` 계정으로 인증돼 있어서(`gh auth status`로 확인 가능) 로그인 절차 없이 바로 push 가능.
- **로컬에서 파일을 아무리 고치고 `generate_wiki.py`를 다시 돌려도, `git add -A && git commit -m "..." && git push`를 안 하면 실제 사이트(위 URL)에는 반영 안 됨.** 작업을 마칠 때마다 잊지 말고 push할 것. (사용자가 명시적으로 "로컬에서만 보겠다"고 하지 않는 한, 의미 있는 변경 후에는 push까지 하는 게 기본 기대치임.)
- 로컬에서 미리보기만 하고 싶으면 `python -m http.server 8000` 실행 후 `http://localhost:8000` (배포와 무관하게 그냥 확인용).

## 파이프라인 구조 (반드시 이 순서로)
1. **`fetch_batch.py --days N`** — 목록 페이지 스크래핑 + 첨부파일(hwp/hwpx/pdf) 자동 다운로드. 순수 파이썬, kordoc 불필요. 이미 받은 파일은 재다운로드 안 함. 결과는 `data/_batch_manifest.json`에 누적 기록.
2. **각 문서마다 (Claude가 직접 해야 함, 스크립트로 자동화 불가):**
   - `mcp__kordoc__parse_document`로 `raw/<boardSeq>.hwpx` 파싱 → 마크다운 텍스트 획득
   - 너무 긴 문서(법령 전문, 수백 행 표 등)는 요약해서 `data/<boardSeq>.md`로 저장 (원본 그대로 베끼지 말 것 — 컨텍스트 낭비)
   - `python generate_wiki.py --ingest <boardSeq>` — manifest에서 제목/날짜 가져오고, 담당부서 표에서 부서명 추출해 `data/<boardSeq>.meta.json` 생성
   - `python generate_wiki.py <boardSeq>` — 실제 위키 HTML(`articles/<boardSeq>.html`) 생성
3. 전부 끝나면 **`python generate_wiki.py --rebuild-index`** — `index.html` + `categories/*.html` 갱신.
4. **`git add -A && git commit -m "..." && git push`** — 이걸 해야 실제로 배포됨 (위 경고 참고).

## 알아둘 것
- **분류 기준**: 목록 페이지의 "담당부서"는 항상 "홍보담당관"이라 못 씀. 반드시 첨부파일 본문 안 "담당 부서" 표에서 실제 부서(예: "학교정책실 공교육진흥과")를 추출해야 함 → `generate_wiki.py`의 `extract_contact()` 함수가 담당. "책임자" 셀 바로 왼쪽 셀을 부서명으로 보는 방식이라 단독/공동 부처 발표 둘 다 대응하지만, **교육부가 아닌 타 부처가 표에서 먼저 나오는 공동 보도자료는 미분류로 빠짐** (예: 문체부·기획예산처 주관 공동자료). 이런 건 수동으로 봐줘야 함.
- **분류 체계**: `category_map.json`에 대분류/중분류 + 부서명→분류 매핑 테이블이 있음. 새로운 부서명이 나오면 (예: 조직개편, 새로운 정책관실) 여기에 추가하면 됨.
- **오래된 문서(2024년 등)**: 그때 조직도가 지금과 달라서 미분류로 빠지는 게 정상. 필요하면 `category_map.json`에 과거 조직명도 추가 가능.
- **원본 파일 위치**: `raw/<boardSeq>.hwpx` (또는 hwp/pdf). 목록 URL: `https://www.moe.go.kr/boardCnts/listRenew.do?boardID=294&m=020402&s=moe`

## 다음에 이어서 할 만한 것
- 더 이전 날짜(예: 2026-06월 이전) 보도자료 추가 (새 문서에도 background 문단 + 필요시 KEYWORD_LINKS 등록 잊지 말 것)
- 미분류 몇 건 수동 재분류 또는 category_map.json 보강
